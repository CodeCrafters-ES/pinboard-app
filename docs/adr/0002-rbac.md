# ADR-002 — Control de acceso basado en roles (RBAC) + RLS

**Estado:** Aceptado  
**Fecha:** 2026-06-17  
**Autores:** Alex Zapata  
**Issues:** [EPIC-S00 #28](https://github.com/CodeCrafters-ES/pinboard-app/issues/28) · [I-F-S00-03-01 #36](https://github.com/CodeCrafters-ES/pinboard-app/issues/36) · [I-F-A00-02-01 #53](https://github.com/CodeCrafters-ES/pinboard-app/issues/53) · [I-F-A00-02-02 #54](https://github.com/CodeCrafters-ES/pinboard-app/issues/54)

---

## Contexto

El proyecto necesita un modelo de autorización que:

- Cubra una app móvil con tres categorías de usuarios (staff, mandos intermedios y administradores).
- Garantice que ningún cliente (React Native / SDK de Supabase) pueda saltarse las reglas por error, aunque las claves de acceso queden expuestas.
- Sea auditable y versionado como parte del código (no como configuración en un panel de control).
- Permita escalar las reglas sin reescribir el cliente.

Decisiones previas que condicionan este ADR:

- **Base de datos:** Supabase Postgres 15 (Row Level Security nativa).
- **Autenticación:** Supabase Auth — `auth.uid()` es el identificador de sesión de confianza.
- **Helpers SECURITY DEFINER:** definidos en la migración `0003` (ver `supabase/migrations/20260617190000_security_definer_role_helpers.sql`).

---

## Decisión

### Roles

Tres roles fijos en el MVP con jerarquía inclusiva `admin > manager > staff`:

| Rol | Descripción |
|---|---|
| `admin` | Superusuario funcional: gestión de personas, contenidos críticos y configuraciones. |
| `manager` | Liderazgo de equipo: analytics agregado, contenidos propios, moderación limitada. |
| `staff` | Usuario base: feed, reacciones, valoraciones, comentarios, chats, eventos. |

### Fuente de verdad

El rol efectivo se almacena **únicamente** en `public.profiles.role` (enum `user_role`). No se replica en JWT claims. Cada request lo resuelve en base de datos.

### Lectura del rol en RLS

`auth_role()` (`SECURITY DEFINER`, `search_path = public, auth`) es el único punto de lectura de `profiles.role` para las policies. Sus wrappers `is_admin()`, `is_manager()` e `is_staff()` son también `SECURITY DEFINER` con `search_path` fijado (migración `0017`). Los cuatro helpers tienen `EXECUTE` revocado a `PUBLIC` y concedido solo a `authenticated` y `service_role`.

```sql
-- Firmas canónicas — estado final tras migraciones 0003 + fix_auth_role + 0017
CREATE OR REPLACE FUNCTION public.auth_role()
RETURNS public.user_role
LANGUAGE sql STABLE SECURITY DEFINER
SET search_path = public, auth
AS $$ SELECT role FROM public.profiles WHERE user_id = auth.uid(); $$;
-- Devuelve el rol del usuario autenticado; NULL si no hay sesión.

CREATE OR REPLACE FUNCTION public.is_admin()
RETURNS boolean
LANGUAGE sql STABLE SECURITY DEFINER
SET search_path = public, auth
AS $$ SELECT COALESCE(public.auth_role() = 'admin', false); $$;
-- True solo para admin. Nunca devuelve NULL.

CREATE OR REPLACE FUNCTION public.is_manager()
RETURNS boolean
LANGUAGE sql STABLE SECURITY DEFINER
SET search_path = public, auth
AS $$ SELECT COALESCE(public.auth_role() IN ('admin', 'manager'), false); $$;
-- True para admin y manager (jerarquía inclusiva). Nunca devuelve NULL.

CREATE OR REPLACE FUNCTION public.is_staff()
RETURNS boolean
LANGUAGE sql STABLE SECURITY DEFINER
SET search_path = public, auth
AS $$ SELECT COALESCE(public.auth_role() IN ('admin', 'manager', 'staff'), false); $$;
-- True para cualquier rol autenticado. Nunca devuelve NULL.
```

### Autorización

La autorización **vive en las RLS policies de Postgres**, nunca en el cliente. El cliente puede ocultar elementos de UI, pero si la policy falla la query falla. Todo cambio de permisos exige:

1. Actualizar la matriz en este ADR.
2. Nueva migración SQL en `supabase/migrations/`.
3. Tests pgTAP (≥ 1 positivo + 1 negativo por policy) en `supabase/tests/rls/`.
4. PR con los tres entregables juntos.

### Bloqueo entre usuarios (I-F-N07-04-02, #288)

**Decisión final:** el modelo `user_blocks` **se incluye en el MVP**. Se prefiere una tabla de pares
`(blocker, blocked)` sobre un flag en `profiles` porque el bloqueo es una relación dirigida entre dos
usuarios, no un atributo de uno. El bloqueo es **mutuo a efectos de interacción** (si A bloquea a B,
ninguno puede escribir al otro) pero **asimétrico a efectos de gestión** (solo A ve y deshace su
bloqueo; el admin puede gestionar cualquiera). Preserva el historial: bloquear no borra mensajes ni
elimina el chat, solo corta el envío de nuevos y la apertura de DMs nuevos. Ver matriz y SQL de
`user_blocks` más abajo.

---

## Matriz de permisos — Base de datos

> **Convención de policy names:** `<tabla>_<accion>_<rol>` — p.ej. `posts_insert_admin_manager`.

### `profiles`

| Acción | Admin | Manager | Staff | Policy tentativa |
|---|---|---|---|---|
| SELECT | Todas las filas | Todas las filas | Todas (sin `email`/`phone`) | `profiles_select_authenticated` |
| INSERT | ✓ | — | — | `profiles_insert_admin` |
| UPDATE (datos básicos) | Todas | — | Propia | `profiles_update_own_or_admin` |
| UPDATE `role` | ✓ | — | — | `profiles_update_role_admin` |
| DELETE | ✓ | — | — | `profiles_delete_admin` |

### `posts`

| Acción | Admin | Manager | Staff | Policy tentativa |
|---|---|---|---|---|
| SELECT | Todas | Todas | Todas | `posts_select_authenticated` |
| INSERT | ✓ | ✓ (propios) | — | `posts_insert_admin_manager` |
| UPDATE | ✓ | Propios | — | `posts_update_own_or_admin` |
| DELETE | ✓ | Propios | — | `posts_delete_own_or_admin` |

### `post_reactions`

| Acción | Admin | Manager | Staff | Policy tentativa |
|---|---|---|---|---|
| SELECT | Todas | Todas | Todas | `post_reactions_select_authenticated` |
| INSERT | Propia | Propia | Propia | `post_reactions_insert_own` |
| UPDATE | Propia | Propia | Propia | `post_reactions_update_own` |
| DELETE | Propia | Propia | Propia | `post_reactions_delete_own` |

### `post_ratings`

| Acción | Admin | Manager | Staff | Policy tentativa |
|---|---|---|---|---|
| SELECT | Todas | Todas | Todas | `post_ratings_select_authenticated` |
| INSERT | Propia | Propia | Propia | `post_ratings_insert_own` |
| UPDATE | Propia | Propia | Propia | `post_ratings_update_own` |

### `post_comments`

| Acción | Admin | Manager | Staff | Policy tentativa |
|---|---|---|---|---|
| SELECT | Todas | Todas | Todas | `post_comments_select_authenticated` |
| INSERT | Propia | Propia | Propia | `post_comments_insert_self` |
| UPDATE | Todas (moderar) | Propia | Propia | `post_comments_update_own_or_admin` |
| DELETE | Todas (moderar) | Propia | Propia | `post_comments_delete_self_or_admin` |

> Moderación verificada en I-F-N03-02-03 (#166): las 4 policies satisfacen el contrato
> (admin modera todo; autor gestiona lo propio). Tests pos/neg en
> `supabase/tests/rls/rls_post_comments.sql`.

### `events`

| Acción | Admin | Manager | Staff | Policy tentativa |
|---|---|---|---|---|
| SELECT | Todas | Todas | Todas | `events_select_authenticated` |
| INSERT | ✓ | ✓ (propios) | — | `events_insert_manager_or_admin` |
| UPDATE | ✓ | Propios | — | `events_update_own_or_admin` |
| DELETE | ✓ | Propios | — | `events_delete_own_or_admin` |

> Modelo *own* (I-F-N05-01-03, #252): manager solo escribe sus propios eventos; admin sobre cualquiera.
> Eventos huérfanos (`author_id` null tras borrar al autor) solo puede editarlos/borrarlos un admin.
> Detalle en [`docs/rls/events.md`](../rls/events.md).

### `chats` / `chat_participants` / `messages`

| Acción | Admin | Manager | Staff | Policy tentativa |
|---|---|---|---|---|
| `chats` SELECT | Participante | Participante | Participante | `chats_select_participant` |
| `chat_participants` SELECT | Participante | Participante | Participante | `chat_participants_select_participant` |
| `messages` SELECT | Participante | Participante | Participante | `messages_select_participant` |
| `messages` INSERT | Participante · sin bloqueo | Participante · sin bloqueo | Participante · sin bloqueo | `messages_insert_participant` |
| `messages` UPDATE / soft-delete | Propia + moderar | Propia | Propia | `messages_update_own_or_admin` |

> Policies de `chats`, `chat_participants` y `messages` se crean en EPIC-N07 (Chat 1:1).
> `messages_insert_participant` se endurece en I-F-N07-04-02 (#288) con una condición de
> **no-bloqueo** frente al interlocutor (ver `user_blocks` más abajo).

### `user_blocks`

Modelo de bloqueo 1:1 (I-F-N07-04-02, #288). Un par `(blocker, blocked)` impide **iniciar o
continuar** interacciones 1:1 (enviar mensajes y abrir un DM nuevo) **sin destruir el historial**.
Es autoservicio: cada usuario gestiona sus propios bloqueos; el admin puede gestionar cualquiera.

| Acción | Admin | Manager | Staff | Policy |
|---|---|---|---|---|
| SELECT | Todos | Propios (como bloqueador) | Propios (como bloqueador) | `user_blocks_select` |
| INSERT | Cualquiera | Propios (`blocker = auth.uid()`) | Propios (`blocker = auth.uid()`) | `user_blocks_insert` |
| DELETE | Cualquiera | Propios (`blocker = auth.uid()`) | Propios (`blocker = auth.uid()`) | `user_blocks_delete` |

> **SELECT acotado al bloqueador**: el usuario *bloqueado* no puede ver quién le ha bloqueado (la
> fila solo es visible para su autor o para un admin). El chequeo bidireccional necesario para las
> policies de `messages` y la RPC de alta de DM se hace vía el helper `is_blocked` (`SECURITY
> DEFINER`), que lee la tabla ignorando RLS. Detalle en la sección de SQL canónico.

### `engagement_sessions`

| Acción | Admin | Manager | Staff | Policy tentativa |
|---|---|---|---|---|
| SELECT | Todas (dashboard) | Todas (dashboard) | Propia | `engagement_sessions_select` |
| INSERT / UPDATE / DELETE | — (solo Edge Function) | — (solo Edge Function) | — (solo Edge Function) | Sin policy de cliente; escribe con `service_role` |

La escritura en `engagement_sessions` está **prohibida desde clientes** (`anon` y `authenticated`). Solo la Edge Function `track-engagement` puede escribir usando `SUPABASE_SERVICE_ROLE_KEY`, que omite RLS por definición.

### `push_tokens`

| Acción | Admin | Manager | Staff | Policy tentativa |
|---|---|---|---|---|
| SELECT | Propia | Propia | Propia | `push_tokens_select_own` |
| INSERT | Propia | Propia | Propia | `push_tokens_insert_own` |
| UPDATE | Propia | Propia | Propia | `push_tokens_update_own` |
| DELETE | Propia | Propia | Propia | `push_tokens_delete_own` |

Ningún rol —admin incluido— lee los tokens de otro usuario: solo `service_role` (Edge Function `send-push`) los lee y purga. Detalle en [`docs/rls/push_tokens.md`](../rls/push_tokens.md).

---

## RLS Policies — SQL canónico

Expresiones exactas implementadas en las migraciones de `supabase/migrations/`. Las operaciones sin policy (INSERT/UPDATE/DELETE en `engagement_sessions`) son denegación implícita por RLS.

### `profiles`

| Policy | Op | USING | WITH CHECK |
|---|---|---|---|
| `profiles_select_authenticated` | SELECT | `true` | — |
| `profiles_insert_admin` | INSERT | — | `is_admin()` |
| `profiles_update_self_or_admin` | UPDATE | `user_id = auth.uid() or is_admin()` | `(user_id = auth.uid() and role = auth_role()) or is_admin()` |
| `profiles_delete_admin` | DELETE | `is_admin()` | — |

> `WITH CHECK` en UPDATE impide que un no-admin cambie su propio `role`. `auth_role()` lee el rol actual del llamante sin recursión gracias a `SECURITY DEFINER`.

### `posts`

| Policy | Op | USING | WITH CHECK |
|---|---|---|---|
| `posts_select_authenticated` | SELECT | `true` | — |
| `posts_insert_manager_or_admin` | INSERT | — | `is_manager() and author_id = auth.uid()` |
| `posts_update_own_or_admin` | UPDATE | `is_admin() or (is_manager() and author_id = auth.uid())` | `is_admin() or (is_manager() and author_id = auth.uid())` |
| `posts_delete_own_or_admin` | DELETE | `is_admin() or (is_manager() and author_id = auth.uid())` | — |

### `post_reactions`

| Policy | Op | USING | WITH CHECK |
|---|---|---|---|
| `post_reactions_select_authenticated` | SELECT | `true` | — |
| `post_reactions_insert_self` | INSERT | — | `user_id = auth.uid()` |
| `post_reactions_update_self` | UPDATE | `user_id = auth.uid()` | `user_id = auth.uid()` |
| `post_reactions_delete_self_or_admin` | DELETE | `user_id = auth.uid() or is_admin()` | — |

### `post_ratings`

| Policy | Op | USING | WITH CHECK |
|---|---|---|---|
| `post_ratings_select_authenticated` | SELECT | `true` | — |
| `post_ratings_insert_self` | INSERT | — | `user_id = auth.uid()` |
| `post_ratings_update_self` | UPDATE | `user_id = auth.uid()` | `user_id = auth.uid()` |

> Sin policy DELETE por diseño: las valoraciones son permanentes y actualizables (UPSERT).

### `post_comments`

| Policy | Op | USING | WITH CHECK |
|---|---|---|---|
| `post_comments_select_authenticated` | SELECT | `true` | — |
| `post_comments_insert_self` | INSERT | — | `author_id = auth.uid()` |
| `post_comments_update_own_or_admin` | UPDATE | `author_id = auth.uid() or is_admin()` | `author_id = auth.uid() or is_admin()` |
| `post_comments_delete_self_or_admin` | DELETE | `author_id = auth.uid() or is_admin()` | — |

> `post_comments_update_self` (migración 0007) fue sustituida por `post_comments_update_own_or_admin` (migración 0018) para añadir moderación de admin.

### `events`

| Policy | Op | USING | WITH CHECK |
|---|---|---|---|
| `events_select_authenticated` | SELECT | `true` | — |
| `events_insert_manager_or_admin` | INSERT | — | `is_manager() and author_id = auth.uid()` |
| `events_update_own_or_admin` | UPDATE | `is_admin() or (is_manager() and author_id = auth.uid())` | `is_admin() or (is_manager() and author_id = auth.uid())` |
| `events_delete_own_or_admin` | DELETE | `is_admin() or (is_manager() and author_id = auth.uid())` | — |

> `is_manager()` devuelve `true` también para `admin` (jerarquía inclusiva). Modelo *own* alineado con `posts`
> (I-F-N05-01-03, #252): las policies permisivas de `20260618400000_rls_events.sql` fueron sustituidas por
> `20260716000001_rls_events_n05_01_03.sql`. Los eventos huérfanos (`author_id` null) solo puede gestionarlos
> un admin, porque el check *own* falla con `null`. Detalle en [`docs/rls/events.md`](../rls/events.md).

### `engagement_sessions`

| Policy | Op | USING | WITH CHECK |
|---|---|---|---|
| `engagement_sessions_select_own_or_manager` | SELECT | `user_id = auth.uid() or is_manager()` | — |

> No existen policies INSERT/UPDATE/DELETE para clientes. La Edge Function `track-engagement` escribe con `service_role`, que omite RLS por definición.

### `user_blocks`

Implementadas en `20260808100000_user_blocks.sql` (#288).

| Policy | Op | USING | WITH CHECK |
|---|---|---|---|
| `user_blocks_select` | SELECT | `blocker_user_id = auth.uid() or is_admin()` | — |
| `user_blocks_insert` | INSERT | — | `blocker_user_id = auth.uid() or is_admin()` |
| `user_blocks_delete` | DELETE | `blocker_user_id = auth.uid() or is_admin()` | — |

> Restricción de tabla `user_blocks_no_self` (`check (blocker_user_id <> blocked_user_id)`): nadie
> puede bloquearse a sí mismo. Sin policy UPDATE por diseño (un bloqueo se crea o se borra).

**Helper `is_blocked(a, b)` — `SECURITY DEFINER`, `STABLE`, `search_path = public`:**

```sql
-- ¿Hay bloqueo entre A y B en cualquier sentido? Lee user_blocks ignorando RLS para que
-- las policies de messages y la RPC de alta de DM comprueben el bloqueo del contraparte
-- (cuyas filas el llamante no puede ver por la policy de SELECT).
CREATE OR REPLACE FUNCTION public.is_blocked(p_a uuid, p_b uuid)
RETURNS boolean LANGUAGE sql SECURITY DEFINER STABLE SET search_path = public AS $$
  SELECT EXISTS (
    SELECT 1 FROM public.user_blocks
    WHERE (blocker_user_id = p_a AND blocked_user_id = p_b)
       OR (blocker_user_id = p_b AND blocked_user_id = p_a)
  );
$$;
```

`direct_chat_blocked(other_user)` es la versión acotada al llamante (`is_blocked(auth.uid(),
other_user)`) que expone la UI, para no permitir sondear pares ajenos. Ambas tienen `EXECUTE`
revocado a `PUBLIC` y concedido solo a `authenticated` y `service_role`.

**Endurecimiento de otras policies con `is_blocked`:**

| Objeto | Cambio |
|---|---|
| `messages_insert_participant` (WITH CHECK) | Añade `and not exists (select 1 from chat_participants cp_other where cp_other.chat_id = messages.chat_id and cp_other.user_id <> auth.uid() and is_blocked(auth.uid(), cp_other.user_id))` — no se envía a un interlocutor bloqueado (en cualquier sentido). |
| `create_or_get_direct_chat(other_user)` | Antes de crear un chat **nuevo**, aborta con `42501` si `is_blocked(me, other_user)`. Un chat ya existente se sigue devolviendo (el historial permanece legible; solo se impide abrir uno nuevo). |

---

## Matriz de permisos — Storage

Los buckets siguen la convención de path `{user_id}/...` para que las policies validen la propiedad sin necesidad de un JOIN.

| Bucket | Admin | Manager | Staff |
|---|---|---|---|
| `avatars` | Upload all / read all | Upload own / read all | Upload own / read all |
| `post-images` | Upload + delete all | Upload + delete own | Read only |
| `event-images` | Upload + delete all | Upload + delete own | Read only |

### Storage Policies — SQL canónico

Todas las policies operan sobre `storage.objects`. La convención de path `{user_id}/...` permite validar propiedad con `(storage.foldername(name))[1]` sin necesidad de JOIN.

#### Bucket `avatars` (público · límite 2 MB · solo `image/webp`)

| Policy | Op | USING | WITH CHECK |
|---|---|---|---|
| `avatars_select_authenticated` | SELECT | `bucket_id = 'avatars'` | — |
| `avatars_insert_own` | INSERT | — | `bucket_id = 'avatars' AND auth.uid()::text = (storage.foldername(name))[1]` |
| `avatars_update_own` | UPDATE | `bucket_id = 'avatars' AND auth.uid()::text = (storage.foldername(name))[1]` | `bucket_id = 'avatars' AND auth.uid()::text = (storage.foldername(name))[1]` |

> Bucket público: cualquier URL del objeto es accesible sin firma. Sin policy DELETE; los avatares se reemplazan vía UPDATE.

#### Buckets `post-images` y `event-images` (privados · límite 5 MB · `image/webp`)

Implementadas en `20260708000000_rls_storage_post_event_images_n02_03_03.sql` (#150). La policy UPDATE soporta el re-upload (`upsert`) sobre el mismo path.

| Policy | Op | USING | WITH CHECK |
|---|---|---|---|
| `post_images_select_authenticated` | SELECT | `bucket_id = 'post-images'` | — |
| `post_images_insert_manager_or_admin` | INSERT | — | `bucket_id = 'post-images' AND (is_admin() OR (is_manager() AND auth.uid()::text = (storage.foldername(name))[1]))` |
| `post_images_update_own_or_admin` | UPDATE | `bucket_id = 'post-images' AND (is_admin() OR auth.uid()::text = (storage.foldername(name))[1])` | `bucket_id = 'post-images' AND (is_admin() OR (is_manager() AND auth.uid()::text = (storage.foldername(name))[1]))` |
| `post_images_delete_own_or_admin` | DELETE | `bucket_id = 'post-images' AND (is_admin() OR auth.uid()::text = (storage.foldername(name))[1])` | — |

> Las mismas expresiones aplican a `event-images` cambiando `bucket_id`. `staff` solo tiene SELECT; las policies de escritura requieren `is_manager()` implícito (admin) o propiedad de carpeta (manager).

---

## Auditoría de cambios de rol

Cualquier cambio en `profiles.role` queda registrado automáticamente en `public.role_audit` mediante el trigger `profiles_role_audit` (función `log_role_change()`, `SECURITY DEFINER`).

| Columna | Descripción |
|---|---|
| `target_user_id` | FK → `profiles(id)` del usuario cuyo rol cambió (`SET NULL` si el perfil se elimina) |
| `changed_by` | `auth.uid()` del actor; `NULL` si el cambio vino de `service_role` |
| `from_role` | Rol anterior (`NULL` en la primera asignación) |
| `to_role` | Nuevo rol |
| `changed_at` | Timestamp del cambio (`DEFAULT now()`) |

**Políticas:**

- Solo `admin` puede `SELECT`.
- No existen policies de INSERT/UPDATE/DELETE para clientes. RLS deniega por defecto, reforzado con `REVOKE`.
- El trigger escribe directamente como `postgres` (SECURITY DEFINER), sin pasar por RLS.

**Retención:** mínimo 2 años desde `changed_at` (ver decisiones de TTL en EPIC-S00).

## Consecuencias

**Positivas:**

- Modelo simple y auditable; toda la autorización en un único lugar (Postgres).
- Un cliente comprometido o mal configurado no puede elevar privilegios.
- Cambios de permisos siempre dejan rastro en el historial de migraciones.

**Negativas / limitaciones conocidas:**

- Jerarquía de roles plana: no hay permisos granulares por recurso individual (ACL fila a fila más allá de RBAC).
- Si el proyecto crece a multi-tenant, este ADR necesita revisión completa (los helpers asumen un solo tenant).
- Los cambios de rol requieren auditoría externa (tabla `role_audit` + trigger definidos en F-S00-05).

---

## Convenciones para nuevos recursos

Al añadir una tabla o bucket nuevo al dominio:

1. Añadir una fila en la sección correspondiente de este ADR.
2. Crear la migración SQL con las policies (nombre: `<tabla>_<accion>_<rol>`).
3. Añadir tests pgTAP en `supabase/tests/rls/<tabla>.sql` (≥ 1 positivo + 1 negativo por policy).
4. Abrir un PR con ADR + migración + tests juntos. No se aceptan policies sin tests ni tests sin policy documentada.

---

## Referencias

- `supabase/migrations/20260617170016_create_user_role_enum.sql` — enum `user_role` + tabla `profiles`
- `supabase/migrations/20260617180000_profiles_role_indexes_and_defaults.sql` — índices y defaults
- `supabase/migrations/20260617190000_security_definer_role_helpers.sql` — helpers `auth_role`, `is_admin`, `is_manager`, `is_staff`
- `supabase/migrations/20260617200000_role_helpers_grants_revokes.sql` — grants/revokes
- `supabase/tests/rls/` — tests pgTAP de las policies
